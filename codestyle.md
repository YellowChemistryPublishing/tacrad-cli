# Style Guide

Elsewise `libcxxext/codestyle.md`.

## Formatting

When deciding what casing to use:

- For macros, `_leading_underscore_snake_case`.
- For concepts, template parameters, types, and global variables, `PascalCase`.
- For _everything_ else, `camelCase`.
